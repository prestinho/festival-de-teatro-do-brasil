import "@testing-library/jest-dom";

import { render, screen } from "@testing-library/react";
import UseLogin from "../UseLogin";

// TODO: create test for UseLogin
describe("tests for component UseLogin", () => {
  test("", () => {
    render(<UseLogin></UseLogin>);
  });
});
