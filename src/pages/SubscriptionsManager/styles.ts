import styled from "styled-components";
import { DefaultButton } from "../../components/DefaultButton/DefaultButton";

export const NewSubscriptionButton = styled(DefaultButton)`
  margin: 15px 0;
`;
