import styled from "styled-components";
import { DefaultButton } from "../../../../components/DefaultButton/DefaultButton";

export const GoogleLoginButton = styled(DefaultButton)``;

export const LoggedButton = styled(DefaultButton)``;
