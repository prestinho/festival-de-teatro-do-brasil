import "@testing-library/jest-dom";

import { render, screen } from "@testing-library/react";
import SaveButton from "../SaveButton";

describe("tests for component SaveButton", () => {
  test("", () => {
    render(<SaveButton></SaveButton>);
  });
});
